class Bird{
    constructor(x, y, num, velocity=0){
            this.x = x;
            this.y = y;  
            this.num = num;     
            this.velocity = velocity;
    }
}

module.exports = Bird;